main {
  "I've called this " + 5 + " times!";
  5 + " o'clock";
  "Lucky number " + 5;
}
------

------
File ([
  FnDef main (
  args: Empty
  statements: [
    StrConcat [Type string] ([
      Literal [Type string] (I've called this )
      Literal [Type int] (5)
      Literal [Type string] ( times!)
    ])
    StrConcat [Type string] ([
      Literal [Type int] (5)
      Literal [Type string] ( o'clock)
    ])
    StrConcat [Type string] ([
      Literal [Type string] (Lucky number )
      Literal [Type int] (5)
    ])
  ])
])
