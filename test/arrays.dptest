use puts(string);

main {
  x = [1,4,9,16,25];
  for(i in 0..4) {
    puts(x[i]);
  }
}
-----

-----
File ([
  UseStatement puts ([
    [Type string]
  ])
  FnDef main (
  statements: [

    ReturnStatement (Literal [Type int] (0))
  ])
])
