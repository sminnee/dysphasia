use puts(string);

/*
 * This is a multi-line commetn
 * Both * and / can appaear inside it but not together
 */
// This is a sweet comment

main {
  return /* this is an intermediate comment */ 0;
}
------

------
File ([
  UseStatement puts ([
    [Type string]
  ])
  FnDef main (
  statements: [
    ReturnStatement (Literal [Type int] (0))
  ])
])
